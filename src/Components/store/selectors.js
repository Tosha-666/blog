export const isAuthorized = (state) => state.user.isAuthorized
export const error = (state) => state.user.error
export const email = (state) => state.user.email
export const load = (state) => state.user.loading
export const username = (state) => state.user.username
export const image = (state) => state.user.image
